# Finio backend API base URL (PHP + MySQL REST API).
# Defaults to https://api.finio.slowatcoding.com when unset (see src/services/api.ts).
# Copy this file to `.env` and override if your backend is hosted elsewhere.
EXPO_PUBLIC_API_URL=https://api.finio.slowatcoding.com
